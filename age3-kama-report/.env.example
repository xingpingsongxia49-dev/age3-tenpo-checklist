# 入店PIN（アプリ全体）。未設定なら 1959 が使われます
APP_PASSCODE=1959

# 管理PIN（設定画面だけ）。未設定なら 3030 が使われます
ADMIN_PASSCODE=3030

# Vercel Postgres / Neon を接続すると日報が永続化されます。
# 未設定でもアプリは動きます（端末内 localStorage に保存）。
POSTGRES_URL=
